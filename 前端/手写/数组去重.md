# 给定一个数组，里面包含重复的数字或字符串，请写一个函数返回去重后的数组。

``` Javascript
// es6 ... 扩展运算符
const unique1 = (arr) => {
  return [...new Set(arr)]
}

const unique2 = (arr) => {
  const res = arr.filter((item, index, arr) => {
    return arr.indexOf(item) === index;
  })
  return res;
}

const unique3 = (arr) => {
  const hash = new Map();
  const res = [];
  for (const val of arr) {
    if (!hash.has(val)) {
      hash.set(val, true)
      res.push(val);
    }
  }
  return res;
}
```